#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@strapi/generators/node_modules/handlebars/bin/handlebars" "$@"
  ret=$?
else 
  node  "$basedir/../@strapi/generators/node_modules/handlebars/bin/handlebars" "$@"
  ret=$?
fi
exit $ret
